/**
 * @planningos/shared — shared types and validation schemas.
 *
 * Placeholder for Batch 1.1 (repo scaffold). Real shared types (tenant, user,
 * project, document DTOs and validation schemas) are added in later batches as
 * the API surface is built. Nothing imports this package yet by design — see
 * 04_PHASE_PLAN.md Batch 1.1 "Do not" fence (no business logic this batch).
 */

/** Marker so the package compiles to a non-empty module. Remove once real exports exist. */
export const SHARED_PACKAGE_NAME = '@planningos/shared' as const;
